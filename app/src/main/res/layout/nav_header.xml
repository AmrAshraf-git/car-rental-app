<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:padding="14dp"
    android:background="@drawable/bg_gradient"
    android:gravity="start"
    android:orientation="horizontal"
    android:weightSum="4"
    android:theme="@style/ThemeOverlay.AppCompat.Dark">

    <ImageView
        android:id="@+id/navHeader_imageView"
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="1"
        android:clickable="true"
        android:contentDescription="account_picture"
        app:srcCompat="@drawable/ic_baseline_account_circle"
        android:focusable="true" />

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="match_parent"
        android:layout_weight="3"
        android:weightSum="2"
        android:orientation="vertical">
        <TextView
            android:id="@+id/navHeader_txtView_title"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:textSize="18sp"
            android:layout_weight="1"
            android:text="Create an account"
            android:paddingStart="5dp"
            android:paddingEnd="5dp"
            android:gravity="start|bottom"
            android:clickable="true"
            android:focusable="true"
            android:textAppearance="@style/TextAppearance.AppCompat.Body1" />

        <TextView
            android:id="@+id/navHeader_txtView_subTitle"
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"
            android:paddingStart="5dp"
            android:paddingEnd="5dp"
            android:clickable="true"
            android:focusable="true"
            android:gravity="start|top"
            android:text="or sign in" />


    </LinearLayout>
</LinearLayout>